/**
 * This .babelrc is for running gulp tasks and webpack.
 * 
 * For compiling web code by webpack, the babel settings are in the config files
 * in the ./webpack folder.
 *
 * For jest testing, the settings in test environment is used.
**/
{
  "presets": [
    ["env", {
      "targets": {
        "node": "current"
      }
    }], 
    "stage-3"
  ]
}