<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <title>InsightFinder</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="shortcut icon" href="<%= require('../images/favicon.png') %>" type="image/x-icon">
    <% for (var css in htmlWebpackPlugin.files.chunks.commons.css){ %>
    <link href="<%= htmlWebpackPlugin.files.chunks.commons.css[css] %>" rel="stylesheet">
    <% } %>
    <% for (var css in htmlWebpackPlugin.files.chunks.app.css) { %>
    <link href="<%= htmlWebpackPlugin.files.chunks.app.css[css] %>" rel="stylesheet">
    <% } %>
    <script>
        window.API_BASE_URL = 'https://insightfinderui.appspot.com/api/v1/';
    </script>
</head>
<body>
<script src="<%= htmlWebpackPlugin.files.chunks.commons.entry %>"></script>
<script src="<%= htmlWebpackPlugin.files.chunks.app.entry %>"></script>
</body>
</html>