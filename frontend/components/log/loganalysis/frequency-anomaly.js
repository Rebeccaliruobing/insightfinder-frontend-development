import React, { PropTypes as T } from 'react';
import './logevent.less';

class FrequencyAnomaly extends React.Component {
  static propTypes = {
  };

  static defaultProps = {
  };

  constructor(props) {
    super(props);

    this.state = {
    };
  }

  render() {
    return (
      <div>aaa</div>
    );
  }
}

export default FrequencyAnomaly;
