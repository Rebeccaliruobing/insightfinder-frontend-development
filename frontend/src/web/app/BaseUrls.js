const BaseUrls = {
  Settings: '/settings',
  SettingsProjectList: '/settings/projects',
  ProjectSettings: '/settings/projects/:projectId',
  ProjectWizard: '/settings/project-wizard',
};

export default BaseUrls;
