import React from 'react';
import logo from '../../common/app/images/logo.png';

const Loading = () => (
  <div className="fui loading">
    <img src={logo} width={120} />
  </div>
);

export default Loading;
